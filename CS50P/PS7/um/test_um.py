from um import count

def test_count():
    assert count("um, hello, um, world") ==
    assert count("um...") ==
    assert count("yum") ==
    assert count("yummy") ==
    assert count("um aja um umm sum um") ==
