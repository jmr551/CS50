#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long card = get_long();
    bool visa = false, master = false, checsum = false;

    // Verificamos primero el checksum
    for (int i = 0; i < 16; i++)
    {
        
    }

}