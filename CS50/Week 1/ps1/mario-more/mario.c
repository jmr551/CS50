#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int n = get_height();

    for (int i = 0; i < n; i++)
    {
        for (int j=)
    }
}

int get_height()
{
    do
    {
        n = get_int("Height: ");
    }
    while (n < 1 || n > 8);

    return n;
}